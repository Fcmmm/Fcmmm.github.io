name: Deploy Blog

on:
  push:
    branches: [source]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install

      - name: Build Hexo
        run: npx hexo generate

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: main
          force_orphan: true
          commit_message: 'deploy: ${{ github.event.head_commit.message }}'

      - name: Deploy static files to Aliyun
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.ALIYUN_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H -p "${{ secrets.ALIYUN_PORT }}" "${{ secrets.ALIYUN_HOST }}" >> ~/.ssh/known_hosts
          rsync -az --delete --ignore-errors \
            -e "ssh -p ${{ secrets.ALIYUN_PORT }}" \
            public/ \
            ${{ secrets.ALIYUN_USER }}@${{ secrets.ALIYUN_HOST }}:/www/wwwroot/fcmmm.xyz/main/

      - name: Deploy server files to Aliyun
        run: |
          rsync -az \
            -e "ssh -p ${{ secrets.ALIYUN_PORT }}" \
            server/ \
            ${{ secrets.ALIYUN_USER }}@${{ secrets.ALIYUN_HOST }}:/www/server/music-api/
          ssh -p ${{ secrets.ALIYUN_PORT }} ${{ secrets.ALIYUN_USER }}@${{ secrets.ALIYUN_HOST }} "\
            echo 'GALLERY_PASSWORD=${{ secrets.GALLERY_PASSWORD }}' > /www/server/music-api/.env && \
            echo 'IMAGE_URLS=' >> /www/server/music-api/.env && \
            chmod 600 /www/server/music-api/.env && \
            echo '.env file created'"

      - name: Notify manual restart
        run: |
          echo "⚠️  server/music-api.js 已更新，请在宝塔终端手动重启:"
          echo "    pkill -f 'node.*music-api'"
          echo "    nohup node /www/server/music-api/music-api.js > /dev/null 2>&1 &"