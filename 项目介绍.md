# 个人博客项目介绍（简历用）

> 本文档面向秋招/实习简历与面试准备，覆盖项目名称、技术栈、功能内容、实现细节、数据统计、实施难点与面试问答。
> 文中的 ⚠️ 占位符需要你自行填写实际数据（后台/统计平台查询）。

---

## 一、项目名称与简介

**项目名称：Fcmmm's Blog —— 基于 Hexo 的个人博客 + 自建网易云音乐服务平台**

- 线上地址：https://fcmmm.xyz
- 源码仓库：github.com/Fcmmm/Fcmmm.github.io（`source` 分支开发，`main` 分支为构建产物）
- 一句话简介：独立开发并持续运营的个人技术博客，集成了**自建网易云音乐 API 代理**、**全站悬浮播放器与歌词系统**、**密码保护的私人图库**、**移动端在线写作后台（Decap CMS）**，实现了"推送代码即可全自动多端发布"的完整 CI/CD 链路。

---

## 二、技术栈

| 层次 | 技术 |
|------|------|
| 静态站点生成 | Hexo 8.1.2 + hexo-theme-butterfly 5.3.2（Pug 模板引擎 + Stylus 预处理） |
| 后端服务 | Node.js（**零依赖**原生 `http/https` 模块自研 API 代理，运行于阿里云 ECS） |
| Serverless | Vercel Serverless Functions（OAuth 代理 / 备用图库与音乐 API）、Cloudflare Worker |
| CI/CD | GitHub Actions（构建 → GitHub Pages 部署 → rsync 同步阿里云服务器 → 写入环境变量） |
| 内容管理 | Decap CMS（原 Netlify CMS）+ GitHub OAuth（Vercel 无服务器代理实现无 Secret 泄露的授权） |
| 前端组件 | APlayer 1.10.1 + Meting.js 2.0.1（音乐播放器）、Giscus（评论，GitHub Discussions 驱动）、busuanzi（访问量统计）、PJAX（无刷新页面切换） |
| 图床 | Cloudinary（签名 URL，Worker/Serverless 两种实现） |
| SEO/订阅 | sitemap.xml、atom.xml（RSS）、Open Graph、JSON-LD 结构化数据、abbrlink 短链 |
| 运维 | 阿里云 ECS + Nginx 反向代理 + 宝塔面板、GitHub Pages（双端部署，国内/国外双活） |

---

## 三、项目内容（功能模块）

1. **博客主体**：文章、分类、标签、归档、时间轴；文章字数统计与阅读时长预估；本地搜索（预生成 search.json，纯前端检索）；RSS 订阅与站点地图。
2. **音乐馆**：网易云"我喜欢的音乐"歌单（948 首）完整播放器 —— 歌单列表、随机/循环播放、**实时滚动歌词（原始歌词 + 翻译双语）**、音量记忆、深色模式适配。
3. **全站悬浮播放器**：任意页面右下角迷你播放器，与音乐馆播放器**双向同步**（切换歌曲、歌单状态一致）；播放时在页面底部浮层显示实时滚动歌词，暂停或无歌词时自动隐藏。
4. **私人图库**：密码验证后展示图片列表，双数据源（服务器目录扫描 + Cloudinary 云图床），密码校验全部在服务端完成。
5. **留言板**：基于 Giscus 的评论系统，无需自建数据库，数据落在 GitHub Discussions。
6. **在线写作后台**：Decap CMS 挂载 GitHub 仓库，手机/电脑浏览器直接写 Markdown 并提交，触发自动发布。
7. **暗黑模式、PJAX 无刷新跳转、移动端响应式适配**。

---

## 四、实现细节

### 4.1 部署架构与 CI/CD

```
本地/线上写作 → git push 到 source 分支
    ↓
GitHub Actions 触发
    ├─ npm install + hexo generate 生成静态站点
    ├─ peaceiris/actions-gh-pages 发布到 GitHub Pages（main 分支，force_orphan 保持仓库干净）
    ├─ rsync public/ → 阿里云 /www/wwwroot/fcmmm.xyz/main/（国内加速，域名已备案）
    ├─ rsync server/ → 阿里云 /www/server/music-api/（API 代码热更新）
    └─ 生成 .env（GALLERY_PASSWORD 等敏感信息从 GitHub Secrets 注入，不落仓库）
```

要点：**敏感信息（网易云 Cookie、图库密码）只存在 GitHub Secrets 与服务器 .env 中**；双端部署兼顾国内访问速度与 GitHub 免费托管；本地另有一键脚本 `deploy.bat`（clean → generate → commit → push）。

### 4.2 自建网易云音乐 API 代理（server/music-api.js）

- 为什么自建：网易云官方接口有 **CORS 限制、Cookie 鉴权（MUSIC_U）、防盗链校验**，浏览器无法直接调用，歌词接口无 Cookie 会返回 `-460 Cheating`。
- 实现：零依赖 Node.js `http` 服务监听 `127.0.0.1:3001`，Nginx 将 `fcmmm.xyz/api/music` 反代到本地端口 —— **站点与 API 同源，天然规避 CORS**。
- 细节：
  - `https.Agent` 开启 keepAlive（maxSockets=20）复用连接，降低与网易云之间的握手开销；
  - 请求统一携带 `User-Agent`、`Referer`、`Cookie` 伪装浏览器，规避反爬；
  - `playlist` 接口对网易云原始字段做**格式适配**（`ar`/`al` 歌手专辑字段 → Meting.js 约定的 `artist`/`pic`），返回的 `url`/`lrc` 字段指向自身代理接口，实现"一次请求拿到整个歌单元数据"；
  - `url` 接口返回 **302 重定向**到真实 mp3 地址（优先 320k 高音质源，失败回退兜底源），让浏览器自己处理音频流，代理不转发大文件流量；
  - `lrc` 接口合并原始歌词与翻译歌词（`lrc + tlyric` 拼接为双语 LRC），**无歌词时也返回 200 + 占位行**而非 404（原因见难点 2）；
  - 图库接口 `POST /api/gallery`：密码比对在服务端进行，成功后扫描服务器图片目录并与环境变量中的 Cloudinary 链接合并返回。

### 4.3 播放器与歌词系统（前端）

- 全局悬浮播放器（fixed+mini）注入到所有页面；音乐馆为完整播放器，两实例共享同一个歌单。
- **双播放器同步**：通过自维护的 `window.aplayers` 实例注册表（包装 APlayer 构造函数，实例化/销毁时登记/移除）互相定位对方实例；音乐馆播放 → 悬浮窗切到对应歌曲并暂停（避免双重声音）；悬浮窗播放中进入音乐馆 → 本地播放器只同步歌单与当前曲目、不抢播。
- **歌词链路**：APlayer `lrcType: 3` 异步模式 —— 以 `audio.lrc` 字段为 URL 发起 XHR 拉取 LRC 文本并解析为时间轴数组，播放时按 `timeupdate` 事件定位当前行并滚动。
- **歌词显示规则（本项目自定义增强）**：
  - 500ms 轮询检查当前歌词：为空或仅占位行（"暂无歌词"/"Not available"）→ 隐藏歌词面板；
  - 悬浮窗歌词浮层在**暂停时也隐藏**，保证"播放时只滚动实时对应的歌词"；
  - 音乐馆页通过 CSS 隐藏悬浮窗的固定歌词浮层，避免与页面内歌词区重叠；
  - 修复 Meting.js 传入 `lrcType` 为字符串导致判断失效的问题（构造器包装层统一转数字）。

### 4.4 图库安全设计

- 密码只在服务端校验，前端拿不到图片列表的真实地址来源逻辑；Cloudflare Worker 与 Vercel Function 各实现了一套（带 1 小时模块级缓存），与服务器版互为冗余。
- Cloudinary 签名 URL：`SHA1(public_id + api_secret)` 生成一次性带签名链接，密钥只存 Worker Secrets，不在前端暴露。

### 4.5 写作工作流（Decap CMS + GitHub OAuth）

- Decap CMS 是纯静态前端，通过 GitHub API 读写仓库 `source/_posts`；但浏览器直连 GitHub OAuth 会暴露 Client Secret，因此用 **Vercel Serverless 做 OAuth 代理**：无 code 时 302 到 GitHub 授权页，回调换取 access_token 后以 URL hash 回传给前端。
- 移动端随时写作 → 提交即 push → Actions 自动发布，全程无需电脑。

### 4.6 统计、评论与 SEO

- 访问统计：busuanzi，站点总 UV/PV + 单页 PV 三档展示；
- 评论：Giscus，基于 GitHub Discussions，无数据库、无审核成本；
- SEO：sitemap.xml + atom.xml + Open Graph + 文章结构化数据，加速收录。

---

## 五、数据与浏览量 ⚠️（待你填写实际数字）

- 文章总数：11 篇（截至 2026-08）
- 音乐馆歌单：网易云"我喜欢的音乐"，**948 首**
- 访问量：站点已接入 busuanzi 统计（总 UV / 总 PV / 单页 PV），**具体数字请登录统计后台或查看站点底部实时数据后填写**：
  - 站点总访问量（PV）：⚠️ ____
  - 站点总访客数（UV）：⚠️ ____
  - 日均 PV：⚠️ ____
- 上线时长：约 1 年（2025 年初搭建，持续迭代）⚠️ 按实际情况修改
- 部署频次：每一次写作/改版均走自动化流水线发布 ⚠️ 可按需补充发布次数

> 简历建议写"上线以来累计 PV ____ / UV ____，持续独立维护"，数字要真实、能经得起追问（统计口径、增长情况）。

---

## 六、实施难点与解决方案（真实踩坑，面试好素材）

### 难点 1：网易云接口鉴权与反爬
- **问题**：歌词接口无有效 Cookie 时返回 `{"code":-460,"msg":"Cheating"}`；直连接口存在 CORS 与防盗链限制。
- **解决**：自建同源代理（Nginx 反代 + 伪装 UA/Referer/Cookie），Cookie 通过服务器 .env 管理，过期只改配置不改代码。这也回答了"为什么要自己写一套 API"。

### 难点 2：歌词区"Not available"与真实歌词重叠（前端疑难杂症，复盘价值高）
- **现象**：播放有歌词的歌曲时，歌词区上方始终残留一行静态 "Not available"，与滚动的真实歌词重叠，影响观感。
- **根因排查（多层叠加，逐个击破）**：
  1. APlayer 1.10.1 源码中，歌词异步加载失败（非 2xx）时会写入兜底行 `[["00:00","Not available"]]`；
  2. Meting.js 2.0.1 默认给**所有**播放器（包括迷你悬浮窗）传 `lrcType=3`，导致悬浮窗也有歌词对象，而其 fixed 模式的歌词是**全屏底部浮层**（CSS `position:fixed; z-index:98`），在每页都渲染；
  3. 服务端对无歌词歌曲返回 404，恰好触发 APlayer 的兜底行；
  4. 曾写过的"歌词预加载"脚本把 `audio.lrc` 从 URL 替换成了文本，破坏了 APlayer 的异步 XHR 拉取，反而加剧了兜底行出现；
  5. 用于同步两个播放器的 `window.aplayers` 实例列表**从未被赋值**，相关同步/清理逻辑全部静默失效（无报错、无效果）。
- **最终方案**：服务端无歌词返回 200 + 占位行（不再触发 APlayer 兜底）；前端 500ms 轮询检测"空歌词/占位行/暂停"三种状态 → 隐藏歌词面板，保证"只显示能正常滚动的实时歌词"；音乐馆页用 CSS 屏蔽悬浮窗歌词浮层；封装 APlayer 构造函数统一修复 `lrcType` 类型并维护实例注册表。
- **面试亮点**：展示了"现象 → 分层假设 → 逐层验证 → 最小改动修复"的排查方法论，以及对第三方库源码（APlayer/Meting.js）的阅读定位能力。

### 难点 3：双播放器同步的静默失效
- **问题**：音乐馆与悬浮窗同步脚本一直没生效，但没有任何报错。
- **排查**：定位到实例注册表缺失（难点 2 第 5 点），同步逻辑里的空值判断直接 return，属于"逻辑存在但依赖未建立"。
- **解决**：在 APlayer 包装层统一维护实例列表，注册/销毁钩子齐全后，双向同步、PJAX 清理全部恢复。

### 难点 4：服务器进程管理失控（运维事故，502 复盘）
- **问题**：手动重启 API 后音乐功能整体消失（502 Bad Gateway）。
- **排查链**：`ss -tlnp` 发现 3001 端口被旧进程 `node server.js`（历史版本文件，已不在仓库）占用 → `kill` 后出现**新 PID**，说明有守护进程（宝塔进程守护/PM2）自动拉起旧文件 → 需先删除守护任务再启动新文件 `node music-api.js`。
- **教训**：进程守护配置必须与部署产物一致；rsync 更新代码不等于重启进程，这类操作应纳入 CI/CD（或容器化）；502 排查的标准链路：Nginx 配置 → 反代上游端口监听状态 → 进程与守护关系。

### 难点 5：多环境部署的一致性与网络问题
- GitHub 托管对国内访问慢 → 双端部署（GitHub Pages + 阿里云 rsync）；开发机直连 GitHub 不稳定 → 掌握代理直连切换与后台重试推送等工程手段；敏感配置通过 Secrets/.env 隔离。

---

## 七、面试官可能问到的问题（附参考回答要点）

### Q1：为什么博客要用 Hexo 这类静态生成器，而不是 WordPress/自己写后端？
答：个人博客读多写少，静态站**无需数据库与运行时**，天然扛流量、零注入面、CDN 友好；Hexo 由 Node 驱动，与我的技术栈一致，主题（Butterfly）生态成熟，支持 Pug/Stylus 深度定制。动态能力（评论、统计、图库鉴权、音乐代理）用外挂组件与自建小服务补齐，架构上是"静态优先、按需动态"。

### Q2：为什么不直接让前端调用网易云 API？
答：三个硬约束——**CORS**（浏览器跨域被拦）、**Cookie 鉴权**（MUSIC_U 属敏感凭据，不能下发到前端）、**反爬校验**（UA/Referer 伪造、歌词接口无 Cookie 返回 -460 Cheating）。所以自建代理服务，并借 Nginx 反代让站点与 API 同源，连 CORS 头都省了。这体现了"前端拿不到的秘密放服务端"的安全原则。

### Q3：讲讲歌词功能的完整实现链路？
答：Meting.js 从代理 API 拿到歌单元数据，每首歌的 `lrc` 字段是指向代理的 URL；APlayer `lrcType=3` 模式下异步 XHR 拉取 LRC 文本，正则解析成 `[时间, 歌词]` 数组（支持双时间轴合并翻译），播放时依据 `timeupdate` 计算当前索引并滚动。我的增强：服务端合并原始+翻译双语 LRC；无歌词时服务端返回 200+占位行，前端轮询检测占位/暂停状态来隐藏歌词面板，避免库的兜底行为（"Not available"）干扰显示。

### Q4：两个播放器（音乐馆 + 悬浮窗）怎么同步的？
答：两个 APlayer 实例共享同一份歌单数据（同一个 playlist id，各自独立实例）。通过包装 APlayer 构造函数维护全局实例注册表，用实例事件（`play`/`pause`/`listswitch`）互相驱动：音乐馆播放 → 悬浮窗 `list.switch(同曲目)` 后暂停；悬浮窗播放中打开音乐馆 → 音乐馆仅同步歌单与曲目索引、不自动播放。中间踩过的坑：注册表未初始化导致同步静默失效，后来加了构造器包装层统一管理实例生命周期。

### Q5：你的 GitHub Actions 流水线做了什么？
答：push 到 `source` 分支触发：安装依赖 → `hexo generate` 产出静态文件 → 用 peaceiris/actions-gh-pages 把 public 发布到 `main` 分支（GitHub Pages）→ rsync 同步 public 与 server 目录到阿里云服务器 → 从 Secrets 生成服务器端 .env。核心设计：**source/main 双分支**（源码与产物分离）、**双端部署**（国内速度 + 国外免费托管）、**敏感信息只走 Secrets**。

### Q6：Decap CMS 为什么需要一个 OAuth 代理？
答：Decap CMS 是纯静态前端，要用 GitHub API 写仓库就必须拿到用户的 access_token；OAuth 授权码流程里 Client Secret 不能出现在浏览器代码中。所以用 Vercel Serverless 做代理：未带 code 时 302 跳 GitHub 授权页，回调后代理服务端用 Secret 换 token，再把 token 通过 URL hash 传给前端。这个场景是理解"为什么 Serverless 适合做 OAuth 中间层"的典型例子。

### Q7：图库的密码验证为什么必须放服务端？密码怎么传的？
答：如果放前端，接口地址与逻辑完全暴露，任何人不输密码也能拼出图片列表。现在密码以 POST body 发给服务端比对（比对逻辑与密钥只存在于服务器/Worker Secrets），校验通过才返回图片列表。密码本身不落前端代码。

### Q8：502 Bad Gateway 你会怎么排查？
答：502 是网关错误，说明 Nginx 拿到了请求但上游没响应。排查顺序：确认上游端口是否有进程监听（`ss -tlnp`）→ 进程是否被守护（kill 后出现新 PID 说明有 supervisor 自动拉起，且拉起的可能是旧文件）→ 检查反代配置指向的端口/路径 → 查看服务日志。我在这个项目里真实处理过：旧版 server.js 被宝塔守护进程反复拉起占用 3001，删除守护任务后重启新版才恢复。

### Q9：静态博客怎么加评论和统计这种"动态"功能？
答：评论用 Giscus 托管到 GitHub Discussions（第三方动态服务，前端 SDK 接入）；统计用 busuanzi 的公共统计脚本；图库/音乐鉴权用自建小服务。原则是：**自己不维护数据库与用户系统，动态需求用服务端小接口或第三方服务补**，保持静态站的可维护性与低成本。

### Q10：你的项目还有什么可以优化的地方？
答（参考）：API 服务用容器化/Docker 部署并纳入 Actions 自动重启，杜绝"改了代码忘了重启"；歌词状态轮询可以改成事件驱动（监听 APlayer 事件）+ MutationObserver，减少定时器开销；给代理加请求缓存与限流，防止被滥用；考虑把悬浮窗与音乐馆状态抽成公共 Store 以简化同步逻辑。

### Q11：这个项目你从 0 到 1 是怎么推进的？
答（参考）：先跑通 Hexo + 主题的最小闭环（写作 → 本地预览 → 推送发布）；再解决核心痛点"想在博客里听自己的网易云歌单"，引出自建代理；随后逐步加图库、评论、写作后台；最后把部署收敛成一条自动化流水线。每个阶段都有明确的目标与验证方式，上线后持续迭代（歌词显示、双播放器同步等都是在真实使用中发现并修复的）。

### Q12（可能的追问）：PJAX 是什么？引入它踩过什么坑？
答：PJAX 用 ajax 拉取新页面内容替换 body，配合 History API 更新地址栏，避免整页刷新、提升切换速度。坑在于**第三方组件的生命周期**：切页后旧页面脚本不重跑、DOM 被替换，播放器实例可能重复创建或悬挂 —— 所以需要在 `pjaxSend` 钩子里销毁非悬浮的播放器实例（这也依赖前面说的实例注册表）。

### Q13（可能的追问）：为什么选择 rsync 而不是直接 docker/CI 部署到服务器？
答：静态产物用 rsync 增量同步最简单可靠、可保留远端已有文件（如 .env）；服务器上没有装 CI runner 的诉求。对于 API 这类常驻进程，我也总结了不足：rsync 只更新文件不重启进程，这正是后续要改进的地方（见 Q10）。

---

## 八、简历一页纸描述（可直接摘抄/改编）

**个人博客与音乐平台（Hexo / Node.js / GitHub Actions）｜独立开发维护**
- 基于 Hexo + Butterfly 构建个人博客（fcmmm.xyz），实现双端自动发布：GitHub Actions 一条流水线完成构建、GitHub Pages 部署与阿里云服务器 rsync 同步，敏感配置经 GitHub Secrets/.env 隔离；
- 自研网易云音乐 API 代理（Node.js 零依赖）：解决 CORS、Cookie 鉴权与反爬问题，提供歌单/播放地址/双语歌词接口，mp3 采用 302 重定向、连接池复用优化性能；
- 实现全站悬浮播放器与音乐馆播放器双向同步，自定义歌词系统：双语实时滚动、无歌词/暂停时自动隐藏歌词区；
- 通过阅读 APlayer/Meting.js 第三方库源码，定位并修复"Not available 歌词重叠"等多层叠加的前端疑难问题；
- 集成 Decap CMS 在线写作后台（自建 Vercel OAuth 代理）、密码保护图库（服务端校验 + Cloudinary 签名 URL）、Giscus 评论与访问统计；
- 上线以来累计访问 ⚠️ PV / ⚠️ UV，持续独立维护。

---

> ⚠️ 提醒：音乐 API 服务端还差最后一步重启（旧 server.js 被守护进程占用导致 502）：在宝塔面板删除旧守护任务后，`cd /www/server/music-api && nohup node music-api.js > /tmp/music-api.log 2>&1 &`，再用 `curl -s 'http://127.0.0.1:3001/api/music?server=netease&type=lrc&id=1339271788'` 验证。恢复后再把简历里的线上功能口径对齐。
